# SmartCart 등록 사용자 실시간 인식 시스템
# 설치: pip install -r requirements.txt

# ── 기존 ────────────────────────────────────────────────────────────────────
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0

# ── 신규 (smart_cart_main.py) ─────────────────────────────────────────────
# YOLO (자동으로 torch 의존성 포함)
ultralytics>=8.0.0

# ReID 백본 (ResNet50)
torch>=2.0.0
torchvision>=0.15.0

# ── 서보 제어 (ESP32 시리얼 통신) ────────────────────────────────────────────
pyserial>=3.5
