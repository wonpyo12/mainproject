# robocart_light — RPi4 런타임 의존성
# cv2, numpy 는 apt(python3-opencv, python3-numpy)로 이미 설치되어 있으므로
# venv 는 --system-site-packages 로 만들어 그대로 사용한다. (여기 넣지 않음)
#
# 설치:  pip install -r requirements_pi.txt
onnxruntime>=1.16.0
